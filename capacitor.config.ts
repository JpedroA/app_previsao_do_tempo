import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projetomobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
